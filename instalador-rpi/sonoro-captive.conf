# SONORO Captive Portal — DNS spoofing wildcard
# Se lee por dnsmasq de NetworkManager en modo `shared` (hotspot).
# Redirige TODAS las queries DNS al portal 10.42.0.1.
# Se instala en /etc/NetworkManager/dnsmasq-shared.d/sonoro-captive.conf.
address=/#/10.42.0.1
